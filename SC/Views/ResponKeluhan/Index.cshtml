@model IEnumerable<SC.Models.Respon>

@{
    ViewData["Title"] = "Index";
    Layout = "_LayoutMhs";
}

<h1>Respon</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create</a>
</p>
<table class="table" id="ResponTable">
    <thead>
        <tr>
            <th>
                ID
            </th>
            <th>
                TANGGAL
            </th>
            <th>
               KELUHANID
            </th>
            <th>
               KELUHAN
            </th>
            <th>
               RESPON
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.User.UserId)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.User.UserName)
            </th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResponId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Keluhan.TanggalKeluhan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KeluhanId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Keluhan.KeluhanMahasiswa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResponKeluhan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ResponId }, new { @class = "btn btn-primary" })

                @Html.ActionLink("Remove", "Remove", new { id = item.ResponId }, new { @class = "btn btn-danger" })
            </td>
        </tr>
}
    </tbody>
</table>
